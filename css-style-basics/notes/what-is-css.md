When you don't apply any styles to your html pages, the browser will fallback to it's default css.
A very basic CSS file that makes sure the html content is at least readable.

CSS is a rule-based language — you define rules by specifying groups of styles that should be applied
to a particular element or groups of elements on your web page.
